DATABASE_URL=
DIAFLOW_API_KEY=
OPENAI_API_KEY=
AUTH_SECRET=
REDIS_DB=
APP_API_KEY=
# Magic link URLs are derived from the browser's Origin header at request
# time (see src/app/api/auth/request/route.ts → getBrowserOrigin), so no
# NEXT_PUBLIC_APP_URL is needed. Preview deploys + custom domains "just work".
